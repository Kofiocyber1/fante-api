services:
  - type: web
    name: fante-api
    runtime: node
    buildCommand: npm install
    startCommand: node server.js
    disk:
      name: fante-data
      mountPath: /opt/render/project/src
      sizeGB: 1
    envVars:
      - key: NODE_ENV
        value: production
